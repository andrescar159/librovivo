{% extends "base.html" %}

{% block title %}Moderar Reseñas - LibroVivo{% endblock %}

{% block content %}
<div class="container py-4">
    <div class="row align-items-center mb-4">
        <div class="col-md-8">
            <h2 class="text-danger">
                <i class="bi bi-shield-exclamation"></i> Moderar Reseñas
            </h2>
            <p class="text-muted mb-0">Gestión de reseñas publicadas por los usuarios</p>
        </div>
        <div class="col-md-4 text-md-end">
            <a href="{{ url_for('dashboard.index') }}" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left"></i> Volver al Inicio
            </a>
        </div>
    </div>

    <!-- Tabs -->
    <ul class="nav nav-tabs mb-4" id="moderarTabs" role="tablist">
        <li class="nav-item" role="presentation">
            <button class="nav-link active fw-bold" id="activas-tab" data-bs-toggle="tab" data-bs-target="#activas" type="button" role="tab">
                <i class="bi bi-check-circle"></i> Reseñas Activas
                <span class="badge bg-primary ms-1">{{ resenas_activas|length }}</span>
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link fw-bold" id="eliminadas-tab" data-bs-toggle="tab" data-bs-target="#eliminadas" type="button" role="tab">
                <i class="bi bi-trash"></i> Eliminadas
                <span class="badge bg-secondary ms-1">{{ resenas_eliminadas|length }}</span>
            </button>
        </li>
    </ul>

    <div class="tab-content" id="moderarTabsContent">
        <!-- Reseñas Activas -->
        <div class="tab-pane fade show active" id="activas" role="tabpanel">
            {% if resenas_activas %}
            <div class="row row-cols-1 g-3">
                {% for r in resenas_activas %}
                <div class="col">
                    <div class="card shadow-sm border-0">
                        <div class="card-body bg-light">
                            <div class="d-flex justify-content-between align-items-start">
                                <div>
                                    <h6 class="mb-1">
                                        {{ r.nombre_usuario|default('Usuario') }}
                                        <span class="text-warning ms-2">
                                            {% for i in range(r.calificacion|default(0)) %}<i class="bi bi-star-fill"></i>{% endfor %}
                                            {% for i in range(5 - r.calificacion|default(0)) %}<i class="bi bi-star"></i>{% endfor %}
                                        </span>
                                    </h6>
                                    <small class="text-muted">
                                        <i class="bi bi-calendar"></i> {{ r.fecha.strftime('%d/%m/%Y') if r.fecha else '-' }} | 
                                        Sobre: <a href="{{ url_for('libros.ver', id=r.libro_id) }}">{{ r.titulo_libro|default('Libro') }}</a>
                                    </small>
                                </div>
                                <form method="POST" action="{{ url_for('resenas.eliminar', id=r.id) }}" class="d-inline" onsubmit="return confirm('¿Eliminar esta reseña?');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger">
                                        <i class="bi bi-trash"></i> Eliminar
                                    </button>
                                </form>
                            </div>
                            <hr class="my-2">
                            <p class="mb-0">{{ r.comentario|default('') }}</p>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
            {% else %}
            <div class="text-center py-5">
                <i class="bi bi-check-circle text-success" style="font-size: 60px;"></i>
                <h5 class="text-muted mt-3">No hay reseñas activas pendientes de moderación</h5>
            </div>
            {% endif %}
        </div>

        <!-- Reseñas Eliminadas -->
        <div class="tab-pane fade" id="eliminadas" role="tabpanel">
            {% if resenas_eliminadas %}
            <div class="row row-cols-1 g-3">
                {% for r in resenas_eliminadas %}
                <div class="col">
                    <div class="card shadow-sm border-0 opacity-75 border-start border-4 border-secondary">
                        <div class="card-body bg-light">
                            <div class="d-flex justify-content-between align-items-start">
                                <div>
                                    <h6 class="mb-1 text-decoration-line-through text-muted">
                                        {{ r.nombre_usuario|default('Usuario') }}
                                        <span class="text-warning ms-2">
                                            {% for i in range(r.calificacion|default(0)) %}<i class="bi bi-star-fill"></i>{% endfor %}
                                            {% for i in range(5 - r.calificacion|default(0)) %}<i class="bi bi-star"></i>{% endfor %}
                                        </span>
                                    </h6>
                                    <small class="text-muted">
                                        <i class="bi bi-calendar"></i> {{ r.fecha.strftime('%d/%m/%Y') if r.fecha else '-' }} | 
                                        Sobre: {{ r.titulo_libro|default('Libro') }}
                                    </small>
                                </div>
                                <form method="POST" action="{{ url_for('resenas.restaurar', id=r.id) }}" class="d-inline">
                                    <button type="submit" class="btn btn-sm btn-outline-success">
                                        <i class="bi bi-arrow-counterclockwise"></i> Restaurar
                                    </button>
                                </form>
                            </div>
                            <hr class="my-2">
                            <p class="mb-0 text-decoration-line-through text-muted">{{ r.comentario|default('') }}</p>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
            {% else %}
            <div class="text-center py-5">
                <i class="bi bi-trash text-muted" style="font-size: 60px;"></i>
                <h5 class="text-muted mt-3">No hay reseñas eliminadas</h5>
            </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}